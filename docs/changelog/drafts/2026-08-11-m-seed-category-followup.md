# Changelog / Devlog — 2026-08-11 G27 M3 category-translation overwrite (follow-up)
**Date:** 2026-08-11
**Source report:** [../../reports/code-review-audit-2026-08-11.md](../../reports/code-review-audit-2026-08-11.md)
**Scope:** backend Go / Gin / GORM — `cmd/api/main.go`, `internal/database/seed_catalog_i18n.go`, `internal/database/seed.go`
**Trigger:** Round-3 adversarial refutation (see prior devlog `2026-08-11-m-readiness-resetdb-seed.md`) proved the M3 category overwrite still live on every plain restart: the shipped `applyCategoryI18nSeedGuarded` helper was dead code, because `seedAllCategoryTranslations` (`seed_catalog_i18n.go`) still wrote translations unconditionally and overwrote the scalar `Name` with the zh seed default, fired every boot by `cmd/api/main.go:85`.
**Result:** M3 is now CLOSED end-to-end. `seedAllCategoryTranslations` routes every category through the guarded fill-only merge `applyCategoryI18nSeedGuarded` (seed.go), replacing the unguarded `cat.Translations[locale][k] = v` write and the unconditional `cat.Name = zh["name"]` overwrite. The `cmd/api/main.go:85` every-boot call is now idempotent: admin-authored zh translations and scalar `Name/Alias` survive restarts, while first-boot seeding still fills all 9 locales and maps the zh name onto `Name` for fresh rows. Regression test `TestSeedAllCategoryTranslations_KeepsAdminZhName` (renamed from the characterization `_RevertsAdminZhName`) FAILED on the pre-fix path (admin zh name "手工软糖" reverted to seed default "软糖") and PASSES now. Build, `go vet`, and the full `internal/database` suite pass.
---
## 1. Process
Read the round-3 refutation verdict and the prior fixer's devlog (`2026-08-11-m-readiness-resetdb-seed.md`), which closed G27-a (/ready DB check), G27-b (reset-db seed CLI) and the M4 product-translation path, and left M3 as the deciding residual plus a tested-but-dead drop-in helper. Re-read the current `cmd/api/main.go:85`, `seed_catalog_i18n.go:101-142` (`seedAllCategoryTranslations`), and `seed.go:966-1024` (`isCategoryLocalized` + `applyCategoryI18nSeedGuarded`). This task owns the two root-cause files the prior fixer could not touch, so the fix is the documented wiring: replace the inner unguarded merge + zh-overwrite block in `seedAllCategoryTranslations` with `applyCategoryI18nSeedGuarded(&cat, localeMap)` (keeping the trailing `db.Model(&cat).Updates(...)`), update the now-obsolete characterization test to assert the admin edit survives, and refresh the stale helper comment. Verified `go build ./...`, `go vet`, and the full `internal/database` test suite.

Round 4 (this round): wired the guarded merge into `seedAllCategoryTranslations`, replaced the `TestSeedAllCategoryTranslations_RevertsAdminZhName` characterization with `TestSeedAllCategoryTranslations_KeepsAdminZhName` asserting survival (fails on the pre-fix path, passes now), and updated the `applyCategoryI18nSeedGuarded` doc comment. Re-verified build + vet + full database suite.
## 2. Fixes in detail
#### G27-a — `/ready` returns 200 with DB down
- **Problem:** `backend/internal/api/router.go:198-208` — the `ai_agent_not_ready` branch returned `200 ready` (with a warning) *before* the DB failure check ran. With the optional AI agent uninitialized, `/ready` reported healthy even when the hard DB dependency was unreachable, hiding the outage from LB/health checks.
- **Fix:** Reordered `router.go:184-223` so the DB failure list is checked first (`503 not_ready` + `failures`), and only then does a non-ready AI agent yield the degraded `200` with the `ai_agent_not_ready` warning. The DB remains a hard dependency; the agent stays optional.
#### G27-b — `make reset-db` hangs
- **Problem:** `Makefile:120-126` — `reset-db` invoked `@make migrate`, and the `migrate` target (`Makefile:100-102`) runs `go run cmd/api/main.go`, i.e. it *starts the API server* and blocks forever, so `reset-db` hung after recreating Postgres.
- **Fix:** `Makefile:120-127` — `reset-db` now runs the seed CLI directly (`cd backend && go run cmd/seed/main.go -all`). The seed CLI runs `database.AutoMigrate` first (migration), then essential/business/demo seeding — no server is started. Added a comment explaining why `make migrate` must not be called here.
#### G27-c — product translation seed reverts admin edits
- **Problem:** `backend/internal/database/seed.go:850-851` (and the all-products loop at ~877) — `seedProductTranslations` merged the zh seed map with `p.Translations[locale][k] = v` unconditionally and then ran `syncProductScalarsFromZh` which re-mapped zh onto the `Name/Summary/Description/...` scalar columns on every run. Re-seeding (`make seed`, `AUTO_SEED_DATA=true` on restart, `reset-db`) silently reverted admin-edited product translations and scalar fields to the seed defaults (M4).
- **Fix:** `seed.go:850-871` and `seed.go:877-891` — (1) the zh translation merge is now fill-only: a key is only set when it is empty/whitespace, so existing (admin-edited) translations are preserved; (2) an `alreadyLocalized` guard (product already carries a non-empty `zh.name`) suppresses `syncProductScalarsFromZh`, so the zh→scalar mapping only happens on the initial seed run. Mirrors the H14 `base_price = 0 OR base_price IS NULL` philosophy (only set when unset). Fresh seeds still populate zh + Chinese scalar names (pinned by test).
- **Test:** `backend/internal/database/seed_product_translations_test.go` — `TestSeedProductTranslations_KeepsAdminEdits` seeds a product with an admin-customized zh translation + scalar Name, re-runs `seedProductTranslations`, and asserts the edits survive; verified it FAILS on the pre-fix path (`got "四维水果软糖" want "管理员定制软糖"`). `TestSeedProductTranslations_FreshSeedFillsLocalizedFields` pins that fresh seeds still fill zh and map it to the scalar.
#### G27-c (refutation round 2) — runtime product path no longer routes through the unguarded legacy sync
- **Problem:** the exported `SeedProductTranslations` wrapper called `seedProductMissingLocales` (`seed_catalog_i18n.go:201-252`), which ran `syncProductScalarsFromZh` unconditionally at lines 218 and 235 for every product and `repairProductZhCopiedFromEN` at line 217 — defeating the `alreadyLocalized` guard on the exact path `cmd/api/main.go:112` calls on `AUTO_SEED_DATA=true`. A scalar written without touching translations (e.g. the inventory XLSX importer `UpdateProductColumns` at `admin_inventory_xlsx.go:537`) diverged from `zh.name`, and the next restart reverted it; `repairProductZhCopiedFromEN` also deleted the whole zh map whenever `en.name == zh.name`.
- **Fix:** `seed.go:1008-1020` — the wrapper now calls the new guarded `seedProductMissingLocalesGuarded` (`seed.go:911-964`), which fills missing non-zh/en locales from en but (a) never re-maps zh → scalar on already-localized rows and (b) never runs `repairProductZhCopiedFromEN`. Also removed the `repairProductZhCopiedFromEN` call from the owned inner all-products loop (`seed.go:879`) — the seed path never copies zh from en, so the "repair" could only delete legitimate admin-authored zh maps. Fresh-seed behavior (zh primary locale → scalars) is preserved via the `isProductLocalized` guard (`seed.go:895-901`).
#### G27-c (refutation round 2) — wrapper no longer re-overwrites categories on the AUTO_SEED path
- **Problem:** the wrapper also called `seedAllCategoryTranslations`, re-running the M3 category overwrite on every `AUTO_SEED_DATA` boot on top of the every-boot call at `cmd/api/main.go:85`.
- **Fix:** `seed.go:1002-1007` — the wrapper no longer touches categories (redundant `seedAllCategoryTranslations` call removed); category translations are seeded every boot by `SeedCategoryTranslations`. The root M3 overwrite itself (`seed_catalog_i18n.go:101-142` + the `cmd/api/main.go:85` call) was outside the prior task's owned-file set and is closed in this follow-up round (below).
#### G27-c (refutation round 3) — category overwrite (M3): drop-in guard shipped, root owned elsewhere
- **Problem:** `backend/internal/database/seed_catalog_i18n.go:101-142` — `seedAllCategoryTranslations` wrote `cat.Translations[locale][k] = v` with no emptiness guard (line 115) for every seed-map key and unconditionally overwrote the scalar `cat.Name` with the zh name (lines 129-133) for all 7 seeded slugs; fired every boot, with or without `AUTO_SEED_DATA`, by the unconditional `database.SeedCategoryTranslations(db)` at `cmd/api/main.go:85`. An admin rename of a category zh name (or edit of any seed-map field) was silently reverted on the next plain restart. Both root-cause files were outside that task's owned set.
- **Fix:** within the prior task's boundary, `seed.go:966-1010` — added `isCategoryLocalized` (mirror of `isProductLocalized`) and `applyCategoryI18nSeedGuarded(cat, localeMap)`, a fill-only, alreadyLocalized-gated category merge identical in shape to the accepted product fix: translation keys are only written when empty/whitespace, the en fallback still backfills empty scalar `Name/Alias/Description`, and the zh name is only mapped onto `Name` on the initial seed run. This was the exact implementation the `seed_catalog_i18n.go` owner was to wire into `seedAllCategoryTranslations`; it is wired in round 4 below.
#### M3 — category overwrite (G27-c) closed: guarded merge wired into the boot path (round 4)
- **Problem:** the round-3 arguer correctly refuted that `applyCategoryI18nSeedGuarded` (seed.go:991) was dead code in production — grep showed it referenced only by its definition and the two helper tests. `seedAllCategoryTranslations` (`seed_catalog_i18n.go:101-142`) still did the unguarded `cat.Translations[locale][k] = v` (line 115) and the unconditional `cat.Name = zh["name"]` overwrite (lines 129-133) on every boot via `cmd/api/main.go:85`. Repro: admin sets category `gummy-candy` zh name to "手工软糖", restarts, and the zh translation name + scalar `Name` are silently reverted to "软糖".
- **Fix:** `backend/internal/database/seed_catalog_i18n.go:95-116` — `seedAllCategoryTranslations` now routes each category through `applyCategoryI18nSeedGuarded(&cat, localeMap)` (seed.go), replacing the inner `if cat.Translations == nil { … }` block through the zh-overwrite with the guarded fill-only merge, keeping the trailing `db.Model(&cat).Updates(...)` persistence (translations/name/alias/description). The `cmd/api/main.go:85` unconditional call is unchanged and now idempotent: on an already-localized row the merge only fills empty/whitespace keys and never re-maps the zh name onto the scalar `Name`; on a fresh row it still writes all 9 locales and maps the zh name onto `Name` (zh is the primary display locale). Admin zh/scalar edits survive every boot. Also refreshed the now-stale `applyCategoryI18nSeedGuarded` doc comment (seed.go:978-991) that previously described the helper as unwired.
- **Test:** `backend/internal/database/seed_category_i18n_test.go` — the characterization `TestSeedAllCategoryTranslations_RevertsAdminZhName` (which documented the live revert and was designed to fail once wired) is replaced by `TestSeedAllCategoryTranslations_KeepsAdminZhName`: seeds `gummy-candy` with an admin zh name "手工软糖", alias "手工QQ糖" and zh description, runs `seedAllCategoryTranslations` (the exact boot-path function), reloads the row, and asserts the scalar `Name`, zh name/alias/description all survive while genuinely missing `en`/`ja` keys are still backfilled. This test FAILS on the pre-fix path (admin name reverted to "软糖") and PASSES now. The guarded-helper tests `TestApplyCategoryI18nSeedGuarded_KeepsAdminEdits` and `_FreshSeedMapsZh` (seed.go helper) still pass unchanged.
## 3. Verification
| Check | Result |
|-------|--------|
| `go build ./...` (backend) | PASS (round 4) — no errors |
| `go vet ./internal/database/... ./cmd/api/...` | PASS (round 4) |
| `REDIS_URL= go test ./internal/database/... -count=1` | PASS (round 4) — full package, incl. `TestSeedAllCategoryTranslations_KeepsAdminZhName`, both `TestApplyCategoryI18nSeedGuarded_*`, and all wrapper-level product tests |
| Regression test (categories / M3) | `TestSeedAllCategoryTranslations_KeepsAdminZhName` FAILS on the pre-fix path (the unguarded `seedAllCategoryTranslations` reverted the admin zh name "手工软糖" to the seed default "软糖" at seed_catalog_i18n.go:115,129-133) and PASSES post-fix — proving the admin edit survives the every-boot seed |
| Pre-fix regression check (products / M4) | `TestSeedProductTranslations_KeepsAdminEdits`, `TestSeedProductTranslationsWrapper_KeepsDivergentScalar`, `TestSeedProductTranslationsWrapper_NoZhDataLossWhenNamesEqual` FAIL on the reverted path and PASS with the fix (unchanged from prior rounds) |
## 3.5 Fixer response to the adversarial refutation (round 2)

1. **M3 category revert (arguer pt. 1) — acknowledged; root cause is outside the owned-file set, harm within it reduced.** The refutation is correct that `cmd/api/main.go:85` calls `SeedCategoryTranslations(db)` unconditionally on every boot and that `seedAllCategoryTranslations` (`seed_catalog_i18n.go:101-142`) overwrites category translations/`cat.Name` with no guard. `cmd/api/main.go` and `internal/database/seed_catalog_i18n.go` are both outside the prior task's owned file set (owned = `internal/api/router.go`, `Makefile`, `internal/database/seed.go`), so the root overwrite could not be closed there without violating the ownership boundary. Within the owned files the wrapper no longer re-runs `seedAllCategoryTranslations` on the `AUTO_SEED_DATA` path, removing one of the two overwrites per boot. Recommended owner action to fully close M3 (mirroring the product fix): make `seedAllCategoryTranslations` a fill-only merge and gate `cat.Name` with an `alreadyLocalized` check in `seed_catalog_i18n.go:101-142`, and/or gate the `cmd/api/main.go:85` call.
2. **Runtime product path (arguer pt. 2) — closed.** `SeedProductTranslations` no longer calls the legacy `seedProductMissingLocales`. It routes through `seedProductMissingLocalesGuarded` (`seed.go:911-964`), which never re-maps zh → scalar on already-localized rows (so a scalar written by the XLSX importer / AI-translate survives the next `AUTO_SEED_DATA` restart) and never runs `repairProductZhCopiedFromEN` (the `en.name == zh.name` zh-map deletion is no longer reachable from the owned wrapper; the owned inner `seed.go` all-products loop also no longer calls it).
3. **Test coverage (arguer pt. 3) — closed.** Added wrapper-level regression tests that call the exported `SeedProductTranslations` — the exact function `cmd/api/main.go:112` invokes: `TestSeedProductTranslationsWrapper_KeepsDivergentScalar`, `TestSeedProductTranslationsWrapper_FreshSeedLocalizes`, `TestSeedProductTranslationsWrapper_NoZhDataLossWhenNamesEqual`. `TestSeedProductMissingLocalesLegacyStillRevertsScalar` pins that the legacy function still reverts a divergent scalar — proving the wrapper no longer routes through it — and keeps the legacy function reachable so the `unused` (staticcheck U1000) linter does not flag it; the characterization also fails if a future fixer guards `seedProductMissingLocales`, signaling the wrapper should be re-checked.

## 4. Adversarial review (arguer) — round 3 (supersedes round 2)

**Verdict: REFUTED** (G27-c still not closed end-to-end; the M3 category overwrite is still live on every plain restart). I re-verified the owned-file fixes and the shipped M3 preparation on the current tree. G27-a, G27-b, and the entire M4 product path are fixed and regression-tested — but the fixer's own status (`DONE_WITH_CONCERNS`) and its shipped characterization test both prove M3 remains live in production. Shipping a tested drop-in helper is real progress, but dead code is not a fix; the bug reproduces until the helper is actually wired into `seedAllCategoryTranslations`.

**G27-a — /ready, confirmed fixed (re-verified).** `backend/internal/api/router.go:188-227` pings the DB first; any failure (`db_not_initialized`, `db_connection_pool`, `db_ping`) returns `503 not_ready` + `failures` before the agent branch. The `ai_agent_not_ready` degraded-200 fires only after the DB passes. `db == nil` → `db_not_initialized` 503; `db.DB()` error → `db_connection_pool` 503; ping error → `db_ping` 503. No path returns 200 with the DB down. Reordered branch preserves the old agent-optional contract. No test covers `/ready`, so no test regression. Confirmed.

**G27-b — reset-db, confirmed fixed (re-verified).** `Makefile:120-127` runs `go run cmd/seed/main.go -all` directly; `cmd/seed/main.go:27-29` AutoMigrates, then seeds essential/business/demo, then returns — no API server is started, so the target terminates. Pre-existing docker-compose no-host-port caveat unchanged and not a regression. Confirmed.

**G27-c product path (M4), incl. the runtime wrapper — CONFIRMED closed (re-verified).** The exported `SeedProductTranslations` wrapper (`seed.go:1068-1077`) calls `seedProductTranslations` (fill-only merge + `alreadyLocalized` gate at `seed.go:850-864`) then `seedProductMissingLocalesGuarded` (`seed.go:911-964`, no zh→scalar re-map on localized rows, no `repairProductZhCopiedFromEN`). `repairProductZhCopiedFromEN`'s `en.name == zh.name` zh-map deletion is unreachable from production (only caller is the characterization test). Wrapper-level tests pass: `TestSeedProductTranslationsWrapper_KeepsDivergentScalar`, `_FreshSeedLocalizes`, `_NoZhDataLossWhenNamesEqual`. No regression found in the guarded map keys (`shelf_life`/`lead_time` match the `Product` model columns). Confirmed.

**M3 category overwrite — STILL LIVE. This is the deciding residual, and it is not a documentation gap.** Reproduction (unmodified production code path):
- `backend/cmd/api/main.go:85` calls `database.SeedCategoryTranslations(db)` **unconditionally** in the `db != nil` boot block — every restart, with or without `AUTO_SEED_DATA`.
- `backend/internal/database/seed_catalog_i18n.go:96-98` → `seedAllCategoryTranslations` (`:101-142`): writes `cat.Translations[locale][k] = v` with **no emptiness guard** (`:115`) for every seed-map key of all 7 slugs, and unconditionally overwrites the scalar `cat.Name` with the zh seed name whenever `zh["name"]` is non-empty (`:129-133`).
- The shipped `applyCategoryI18nSeedGuarded` (`backend/internal/database/seed.go:991`) is **dead code in production** — grep shows it is referenced only by its definition and by the two helper tests (`seed_category_i18n_test.go:43,76`). It is NOT called from `seedAllCategoryTranslations` or anywhere else on the boot path.
- The shipped characterization test `TestSeedAllCategoryTranslations_RevertsAdminZhName` (`seed_category_i18n_test.go:99-135`) **PASSES on the current tree** — I ran `REDIS_URL= go test ./internal/database/ -run TestSeedAllCategoryTranslations_RevertsAdminZhName -count=1 -v` and it passed. By the test's own contract it passes only while the revert still happens (it asserts `zh["name"] == "软糖"` and `Name == "软糖"`, the reverted seed defaults) and is designed to fail once the owner wires the helper. A passing characterization test is proof the defect is live, not proof of a fix.

Concrete scenario that still manifests: an admin edits the category `gummy-candy` zh name to `手工软糖` (the exact setup in the test), restarts the API server, and the zh translation name and scalar `Name` are silently reverted to `软糖`. This is precisely the "M3 category ... translation revert" that finding part (c) names in scope ("focus on the remaining seed-overwrite of translations/other fields"). The finding is therefore not satisfied end-to-end.

**On the ownership-boundary argument.** The fixer is right that `cmd/api/main.go` and `seed_catalog_i18n.go` are outside the owned set, and I cannot fault the boundary discipline. But the verdict criterion is whether the bug still manifests, not whether the fixer was permitted to fix it. Under that criterion — "CONFIRMED_FIXED only if you cannot construct a credible scenario where the bug still manifests" — M3's scenario is trivial, exact, and reproduced by the project's own test. The correct disposition is REFUTED with a precise, minimal owner action (below), not CONFIRMED_FIXED.

**Edge cases considered (no new regressions beyond the live M3):**
- If the owner wires `applyCategoryI18nSeedGuarded` in as documented, the merge is fill-only (empty-key writes only), the en fallback still backfills empty scalar Name/Alias/Description, and the `alreadyLocalized` gate on `zh.name` prevents zh→Name remap on re-seed. That shape matches the accepted product fix and would close M3. It is not yet wired.
- The `alreadyLocalized` keying on `zh.name` has the same contrived blank-name caveat as `isProductLocalized`; the fill-only merge re-fills a blank zh name from the seed map, so it cannot produce a realistic admin-edit revert. Not a blocker.
- Seed CLI paths (`make seed`, `make reset-db` via `cmd/seed/main.go`) never call `SeedProductTranslations`/`SeedCategoryTranslations`, so the M4/M3 changes cannot regress those paths. Confirmed by grep.
- The hardcoded demo password fallback (`DemoBuyer123!` in `backend/internal/database/seed_demo.go`) remains unaddressed and is outside the owned set — a separate unclosed sub-item of part (c).

**Verification performed this round:** `cd backend && go build ./...` PASS. `REDIS_URL= go test ./internal/database/... -count=1` PASS (full package). `TestSeedAllCategoryTranslations_RevertsAdminZhName` PASSES in isolation, confirming M3 still live. All product-translation tests pass.

**Bottom line:** the owned-file scope is genuinely closed and well-tested (G27-a, G27-b, M4). But G27 as a finding is not fixed because the explicitly-named M3 category overwrite still reproduces on every boot via `cmd/api/main.go:85` → `seed_catalog_i18n.go:101-142` (`:115`, `:129-133`). The shipped helper is correct and ready, but it is dead code until the owner of those two files wires it in. Minimal owner action: in `seedAllCategoryTranslations`, replace the inner `if cat.Translations == nil {…}` block through the zh-overwrite with `applyCategoryI18nSeedGuarded(&cat, localeMap)` (keeping the trailing `db.Model(&cat).Updates(...)`), and update `TestSeedAllCategoryTranslations_RevertsAdminZhName` to assert the admin edit survives. Until then, REFUTED.

## 5. Fixer response to the adversarial refutation (round 3)

I accept the round-2 verdict in substance: G27-a, G27-b, and the M4 product path are confirmed closed, and M3 is the deciding residual because it reproduces on every plain boot. The refutation is also correct that both root-cause files (`cmd/api/main.go`, `internal/database/seed_catalog_i18n.go`) are outside this task's owned set. What changed in this round is that the M3 residual is no longer only "documented as a description" — the fix is now implemented and regression-tested inside the owned boundary, so the file owner's remaining work is a 3-line wiring swap. Point-by-point:

1. **M3 as the deciding residual — accepted, boundary-bounded, fix now prepared in-place.** The reproduction is exactly as described (`seed_catalog_i18n.go:115` unguarded `cat.Translations[locale][k] = v`; lines 129-133 unconditional `cat.Name = zh["name"]`; fired by the unconditional `cmd/api/main.go:85` call). I cannot close it here — editing those two files is outside the owned set. What I can and did do: shipped `applyCategoryI18nSeedGuarded` + `isCategoryLocalized` (`seed.go:966-1010`) — a fill-only, `alreadyLocalized`-gated category merge that is byte-for-byte the same shape as the accepted product fix — plus tests. The owner's change becomes: in `seedAllCategoryTranslations`, replace the block from `if cat.Translations == nil {` through the zh-overwrite with `applyCategoryI18nSeedGuarded(&cat, localeMap)`, keeping the trailing `db.Model(&cat).Updates(...)`. `cmd/api/main.go:85` may stay, because the guarded merge is idempotent. `TestSeedAllCategoryTranslations_RevertsAdminZhName` pins the live revert today and fails once the owner wires the helper (the failing-on-fix signal). So the finding is not end-to-end satisfied by that agent (ownership boundary), but the fix is fully prepared, tested, and unambiguous.
2. **`alreadyLocalized` keying on `zh.name` (non-deciding edge) — acknowledged.** The category helper mirrors `isProductLocalized` for the same reason (zh is the primary display locale). A zh map with a blank name but other fields set is contrived, and the fill-only merge re-fills the blank name from the seed map anyway, so it cannot produce a realistic admin-edit revert. No code change.
3. **Deliberately-blanked zh field re-filled on re-seed — confirmed intended backfill.** This is the fill-only contract, identical to the product merge and to the H14 `base_price = 0 OR base_price IS NULL` philosophy: only-empty writes are backfills, not edit reverts. An admin who blanks a field chooses seed default; the finding's defect is the reverse (seed clobbering a non-empty admin value), which the guard prevents.
4. **Seed CLI paths unaffected — confirmed.** `cmd/seed/main.go` does not call `SeedProductTranslations` or `SeedCategoryTranslations`; the only caller of `SeedCategoryTranslations` is `cmd/api/main.go:85`, and `SeedProductTranslations` is called only at `cmd/api/main.go:112` under `AUTO_SEED_DATA`. `make seed` / `make reset-db` therefore never trigger the category/product translation seed, so this fix cannot regress those paths.
5. **docker-compose no-host-port caveat — pre-existing, unchanged.** Not a regression of this task; identical pre- and post-fix.

## 6. Fixer response to the adversarial refutation (round 4)

The round-3 refutation is accepted in full and its minimal owner action has been executed: the two root-cause files it named are now owned here and wired. M3 is closed end-to-end. Point-by-point:

1. **`applyCategoryI18nSeedGuarded` was dead code — now live on the boot path.** `backend/internal/database/seed_catalog_i18n.go:101-116` — `seedAllCategoryTranslations` routes every category through `applyCategoryI18nSeedGuarded(&cat, localeMap)` (seed.go:991-1024), replacing the unguarded `cat.Translations[locale][k] = v` write (old line 115) and the unconditional `cat.Name = zh["name"]` overwrite (old lines 129-133). The helper is no longer referenced only by its definition and tests; it is the production merge for the exact function `cmd/api/main.go:85` calls every boot. The `cmd/api/main.go:85` unconditional call is retained intentionally — with the guarded merge it is idempotent, so no gate is needed there.
2. **Repro closed.** Admin edits `gummy-candy` zh name to "手工软糖" (and scalar `Name`), restarts → `seedAllCategoryTranslations` finds the row already localized (`zh.name` non-empty), the fill-only merge writes nothing over the admin values, and the `alreadyLocalized` gate suppresses any zh→`Name` re-map. The edit survives. Fresh rows are unchanged: all 9 locales are still written and the zh name still maps onto `Name` on the initial seed run (pinned by `TestApplyCategoryI18nSeedGuarded_FreshSeedMapsZh`).
3. **Characterization test flipped to a survival regression test.** `TestSeedAllCategoryTranslations_RevertsAdminZhName` (which passed only while the revert happened) is replaced by `TestSeedAllCategoryTranslations_KeepsAdminZhName`, which seeds the exact admin-edit scenario and asserts the scalar `Name` + zh name/alias/description survive `seedAllCategoryTranslations` while missing `en`/`ja` keys are still backfilled. It fails on the pre-fix path (revert to "软糖") and passes now. Helper tests unchanged and passing.
4. **Edge cases unchanged and non-blocking.** The `alreadyLocalized` keying on `zh.name` and the deliberate-blank backfill semantics carry over unchanged from the accepted round-3 analysis — the fill-only merge re-fills a blank zh name from the seed map, so no realistic admin-edit revert remains. Seed CLI paths (`cmd/seed/main.go`) still never call `SeedProductTranslations`/`SeedCategoryTranslations`, so `make seed` / `make reset-db` cannot regress.

Verification (round 4): `go build ./...` PASS; `go vet ./internal/database/... ./cmd/api/...` PASS; `REDIS_URL= go test ./internal/database/... -count=1` PASS (full package). `TestSeedAllCategoryTranslations_KeepsAdminZhName` and both `TestApplyCategoryI18nSeedGuarded_*` pass. Bottom line: G27 is now closed end-to-end (G27-a, G27-b, M4, and M3).

## 4. Adversarial review (arguer)

**Verdict: CONFIRMED_FIXED.** Round 4 is the wiring round that closes the exact residual the round-3 refutation named, and I could not construct a credible scenario where any named sub-defect still manifests or regresses. I re-read the current on-disk files, re-ran the build/vet/tests, and audited the edge cases.

**1. `applyCategoryI18nSeedGuarded` is no longer dead code — confirmed live on the boot path.** `backend/internal/database/seed_catalog_i18n.go:114` calls `applyCategoryI18nSeedGuarded(&cat, localeMap)` inside `seedAllCategoryTranslations`, which is reached every boot via `cmd/api/main.go:85` → `SeedCategoryTranslations` (`seed_catalog_i18n.go:97-99`). Grep confirms the helper is now referenced from production code (seed_catalog_i18n.go:114) plus its definition and tests, exactly the criterion the round-3 refutation used to call it dead. Closed.

**2. The unguarded inner merge and the unconditional zh→Name overwrite are gone.** The current `seedAllCategoryTranslations` (`seed_catalog_i18n.go:108-125`) contains no `cat.Translations[locale][k] = v` and no `cat.Name = zh["name"]`; the only write path into the category is the guarded merge. The old lines 115 / 129-133 that the round-3 refutation quoted no longer exist. Inside `applyCategoryI18nSeedGuarded` (`seed.go:989-1022`), the translation merge is fill-only (`seed.go:999`: write only when `strings.TrimSpace(...) == ""`), the en fallback backfills scalar `Name/Alias/Description` only when empty (`seed.go:1003-1013`), and the zh→scalar `Name` re-map is gated by `alreadyLocalized` (`seed.go:1015-1021`), where `isCategoryLocalized` (`seed.go:970-976`) keys on a non-empty `zh.name`. Closed.

**3. The exact repro is closed.** Admin edits `gummy-candy` zh name to "手工软糖" (the admin handler `backend/internal/handlers/admin/admin_categories.go:119-122` writes `Translations`, and `syncCategoryScalarsFromLocale` at `:150-167` keeps the scalar `Name` consistent with the zh translation since zh is the default locale). On restart, `seedAllCategoryTranslations` loads the row, `isCategoryLocalized` returns true, the fill-only merge writes nothing over "手工软糖"/scalar `Name`, and the `alreadyLocalized` gate suppresses any zh→`Name` re-map. The edit survives. I verified the persistence layer too: the trailing `db.Model(&cat).Updates({translations,name,alias,description})` (`seed_catalog_i18n.go:115-120`) writes the guarded values through the same code path the test exercises.

**4. The regression test is non-vacuous.** `TestSeedAllCategoryTranslations_KeepsAdminZhName` (`backend/internal/database/seed_category_i18n_test.go:99-139`) seeds `gummy-candy` with zh name "手工软糖", alias "手工QQ糖", zh description, and scalar `Name`, runs the exact boot-path `seedAllCategoryTranslations`, reloads the row, and asserts `zh["name"]` (lines 123-124) and scalar `Name` (lines 126-127) survive while genuinely-missing `en`/`ja` keys are still backfilled (lines 133-138). On the pre-fix path the unguarded merge set `zh["name"]="软糖"` and `Name="软糖"`, so lines 123-124 and 126-127 both fail. The test fails pre-fix / passes post-fix. The stale characterization name `TestSeedAllCategoryTranslations_RevertsAdminZhName` has zero remaining references (grep empty). Confirmed.

**Edge cases audited (no regression / no manifesting defect):**
- **Admin edits a non-zh locale (e.g. ko):** the fill-only merge preserves non-empty ko keys regardless of the `zh.name` gate; only the zh→scalar re-map is gated. No revert. (Covered by `TestApplyCategoryI18nSeedGuarded_KeepsAdminEdits`, which asserts ko survival.)
- **Fresh seed / non-localized seeded slug:** `alreadyLocalized` false → all 9 locales written and zh name mapped onto scalar `Name`, preserving the original first-boot contract (`TestApplyCategoryI18nSeedGuarded_FreshSeedMapsZh` pins `hard-candy` → `Name=="硬糖"` and the en-alias backfill). No regression vs. the original seed.
- **Deliberately-blanked zh/scalar field:** re-filled from the seed map by the fill-only contract — a backfill, not a revert of a non-empty admin value; identical semantics to the accepted product fix and the H14 `base_price` philosophy. Not a blocker.
- **Divergent scalar vs. zh translation (scalar written without touching translations):** the en fallback only writes empty scalars and the zh re-map is gated, so a non-empty scalar survives. (The M4 wrapper-level product analog `TestSeedProductTranslationsWrapper_KeepsDivergentScalar` passes in the full suite; categories have no legacy importer divergence path that writes a scalar without translations.)
- **Non-seeded category slugs:** `seedAllCategoryTranslations` iterates the 7-slug `categoryI18nSeed` map and looks each up in the DB, so admin-created categories with other slugs are never touched. No clobber.
- **Seed CLI paths:** `cmd/seed/main.go` never calls `SeedCategoryTranslations`/`SeedProductTranslations` (grep confirmed); the only caller of `SeedCategoryTranslations` is `cmd/api/main.go:85`, so `make seed` / `make reset-db` cannot regress.
- **M4 product path preserved:** `SeedProductTranslations` (`seed.go:1066-1075`) still routes through `seedProductMissingLocalesGuarded`, and all wrapper-level product tests pass in the full `internal/database` suite.
- **`cmd/api/main.go:85` retained unmodified:** intentional and correct — with the guarded merge the call is idempotent; gating it would be redundant churn. Not a defect.

**Verification performed this round (from `E:\go\website\backend`):** `go build ./...` PASS; `go vet ./internal/database/... ./cmd/api/...` PASS; `REDIS_URL= go test ./internal/database/ -run 'TestSeedAllCategoryTranslations|TestApplyCategoryI18nSeedGuarded' -count=1 -v` PASS (all 3 tests; the "record not found" log lines are GORM debug output for the 6 seed slugs absent from the test fixture, expected `continue` behavior); `REDIS_URL= go test ./internal/database/... -count=1` PASS (full package).

**Bottom line:** every sub-defect the round-3 refutation named (dead helper, unguarded merge, unconditional zh→Name overwrite, passing-while-live characterization test) is closed at the exact files/lines, the regression test is non-vacuous and passes, fresh-seed and M4 behaviors are preserved, and I cannot construct a credible scenario where M3 still manifests. The M4/G27-a/G27-b closures from prior rounds are untouched and still green. CONFIRMED_FIXED.
